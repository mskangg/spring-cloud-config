server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mskangg/spring-cloud-config-yml
          force-pull: true

  application:
    name: baemin
  profiles:
    active: dev,stg,qa,prod
